[project]
name = "analyzer"
version = "0.1.0"
description = "AI Analyzer for the O.D.D Device"
authors = [
    {name = "eliteabhi",email = "abhirangarajan2003@gmail.com"}
]
license = {text = "MIT"}
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
